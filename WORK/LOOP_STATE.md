# Ascend Autonomous Loop — State

Machine-updated by loop iterations per `WORK/LOOP_PROTOCOL.md`. Humans: edit
the backlog freely; the loop treats your edits as authoritative.

## Heartbeat

| Field | Value |
|---|---|
| loop_status | STOPPED (refreshed 2026-07-30) — no autonomous loop iteration is running. Everything since iteration 20 (2026-07-19) was direct, Sri-directed interactive work across multiple sessions, not loop-selected. Per `docs/architecture/ORCHESTRATION.md`'s autonomous loop protocol pointer: a stopped loop is success, not failure, when the verified backlog is empty pending a human decision — see the refreshed backlog below, which is genuinely near-empty of loop-selectable items right now. |
| last_iteration_utc | 2026-07-19T190000Z (iteration log below unchanged through #20 — historical record, not rewritten). Everything after that date is summarized in a new consolidated entry rather than back-filled iteration-by-iteration, since it wasn't run through loop machinery. |
| runner | none active. Four AI environments now coordinate on this repo (Claude Code sessions, Cursor, Replit) per `docs/architecture/ORCHESTRATION.md`'s "Environment routing" section (added 2026-07-30) — GitHub Issues + PRs are the durable coordination surface, not this loop, for anything after 2026-07-19. |
| branch | n/a — work happens on short-lived `feature/*`/`chore/*`/`docs/*` branches per PR now, not one long-lived loop branch. `feature/web-error-details` (referenced below) is historical. |
| idle_streak | n/a (not loop-tracked since 2026-07-19) |
| loop_commits | n/a (not loop-tracked since 2026-07-19) |
| focus | **No standing loop focus right now.** The live, active thread as of 2026-07-30 is the production-infrastructure reconciliation in `docs/architecture/DEPLOYMENTS.md` (gated on Sri confirming Render/Vercel/Supabase dashboard facts — see that doc's P0-P3 fix plan) — not a loop-selectable backlog item, a Sri-decision-gated investigation. |

### Why stopped
Four systematic verification sweeps complete, all retail-core modules:
1. Route drift (iters 1,4) — fixed 4 prod 404s.
2. Unbounded-list pagination (iters 1,5) — fixed movements/audit/journal.
3. Authorization (iters 6,7,8) — fixed sync/reports/ecommerce/notifications.
4. Tenant-scoping (iter 9) — **VERIFIED CLEAN, no cross-tenant leaks**: every
   literal `WHERE id=@id` mutation is gated by a prior `WHERE id AND tenant_id`
   verify (verify-then-mutate) or re-reads a just-created row; dynamic `${where}`
   builders include tenant_id; RLS backstop underneath.

No autonomous high-value work remains. Further progress = feature development
(needs Sri to choose scope) — the loop's explicit "needs Sri decision" stop.
**To resume:** `/loop` with a specific initiative, or merge PR #70 first.

**Refreshed 2026-07-30**: still true, more so. Everything loop-selectable from the
old backlog is `DONE`; everything remaining is `NEEDS-SRI` (product/infra decisions)
or actively blocked on Sri confirming external (Render/Vercel/Supabase dashboard)
facts — see `docs/architecture/DEPLOYMENTS.md`. No code-addressable backlog exists
right now. Don't resume the loop against this file without adding a real initiative
first; there's nothing here for it to pick up.
| last_merge | 2026-08-11 — **PR #210 → `develop` (`ad64c4a`)**, then PR #211 → `develop` (`31c974c`, separate session). PR #210 merged with a **real merge commit, deliberately not a squash**: its first commit `d58c7e5` is the back-merge that restores `develop`'s ancestry, and a squash would have flattened it onto develop's old tip — which did not contain `master` — silently reverting the invariant the PR existed to fix. **The `develop ≥ staging ≥ master` invariant is now repaired and verified:** `master` and `staging` are both ancestors of `develop`, 0 behind each (before: `master` missing 57 commits, `staging` 66). Open: **PR #220** (`develop → staging`, draft) is the remaining promotion — it carries PR #209's two files plus everything from #210 and #211. Prior history: 2026-07-30 PR #125 (`7b13f1d`), order #124 → #117 → #127 → #126 → #125, all `develop`-only. |
| cloud_watchdog | trig_01VVXryUgSBHoy9mAqRdhfzz (notify-only, every 3h, emails on ≥3h stale heartbeat) — unchanged, not re-verified this session |

## Iteration log

| # | UTC | Commit | Summary |
|---|---|---|---|
| — | 2026-08-11T033000Z | ad64c4a | **DELIVERED — PR #210 merged to `develop` after a full pre-merge audit.** Carries three separable pieces: the `staging`→`develop` back-merge, the design-system foundation, and three migrated routes (`/orders`, `/customers`, `/inventory`). **Merged as a real merge commit, not a squash** — `d58c7e5` is the back-merge restoring ancestry, and squashing would have parented it on develop's old tip (which did not contain `master`), silently reverting the invariant. Verified after merge: `master` and `staging` are both ancestors of `develop`, 0 behind each. **Validation actually run, not asserted:** 899/899 backend tests, 234/234 web tests, backend + web typecheck, ESLint, production build (123/123 routes), `hygiene-check`, `gap:scan`, `authz:scan`, `prevent:drift`, and **browser QA** on all three routes at 1280/1366/1440/1920 — no horizontal overflow, no page errors, no unlabelled controls, tokens resolving from the new layer. CI fully green on `01656e8` (17 checks) before merge. **Two things the audit itself caught and fixed before merge:** (1) `gap:scan` failed because migrating `/inventory` inlined a template into `apiGet()`, exposing it to the scanner's prefix check — fixed by keeping the URL in a variable, after confirming the "obvious" alternative traded one scanner failure for another; (2) browser QA found `/orders`'s empty state naming the Register with no way to reach it, which for a first-run tenant *is* the whole experience. **One local backend failure was diagnosed, not waved away:** `ai-assistant.test.ts` asserts an internal create-PO call fails because nothing listens at `BACKEND_URL` (defaults to `http://localhost:3000`) — my own dev server was on that port for the visual QA. Killed it, re-ran, 899/899. That test is coupled to port 3000 being free and will break for anyone running a dev server; worth binding a known-dead port instead. **Remaining:** PR #220 (`develop → staging`) open as a draft — not merged, authorisation covered `develop` only. Phase 3 continues with dashboard, terminal, catalog, purchasing, receiving; the Phase 5 lint gate that stops the raw-palette system regrowing is still unbuilt. |
| — | 2026-08-10T190500Z | (ui-phase3-inventory) | **UI/UX Phase 3, third route — `/inventory`, and it was carrying four real bugs behind the styling.** Visual: 23 raw-palette classes and every `[#hex]` arbitrary → **0**; hand-rolled `<table>`, 7 `<input>`, 2 `<select>` and a bare-div modal → `DataTable`/`Input`/`Select`/`Modal`; skeleton loading replaces a bare spinner (this page had **zero** loading-state markers, the worst in the audit). **Bug 1 — the summary reported unknown as zero.** Purchase orders have no quantity column (`listOrders` is `SELECT * FROM purchase_orders`) and transfers carry no cost, but both normalised to `0`, so the Orders tab asserted "total qty **0**" and Transfers "total cost **$0.00**" — on the page whose whole job is answering *what is in stock and what did it cost*. Both fields are now `number \| null`, aggregation skips `null`, and the UI says "quantity not tracked for orders". A genuine zero still sums as zero; that case is asserted so the fix can't overshoot. **Bug 2 — supplier names came from a hard-coded map of two demo ids** (`sup_acme` → "Acme Coffee Co"), so every real supplier rendered as a raw `sup_…` id; now resolved from the `/api/v1/purchasing/vendors` endpoint that already existed, falling back to the id on failure. **Bug 3 — "More filters" revealed a date input wired to nothing**; now a real "Created from" filter. **Bug 4 — the create dialog was a bare `fixed inset-0` div**: no focus trap, no `role="dialog"`, no Escape, no focus restore, while the repo's `Modal` (a real `<dialog>` + `showModal()`) gives all four; validation also moved from a toast to inline per-field errors, since a toast can't say *which* field and vanishes before you reach it. **Caught mid-task:** resolving supplier names inside `normalize` made `load` change identity when the vendor map arrived and **refetched the entire list a second time** — names now resolve at render and `normalize` is pure. **Caught by the build:** Next.js forbids arbitrary named exports from `page.tsx`, so the logic moved to `_lib/movements.ts`, where it is unit-tested (8 new tests). Gates: typecheck, lint, build (123/123), **234/234 tests** (31 files), hygiene — all green. **Remaining Phase 3:** dashboard, terminal, catalog, purchasing, receiving. Still no browser/visual QA on any of it. |
| — | 2026-08-10T185000Z | (ui-phase3-customers) | **UI/UX Phase 3, second route — `/customers` migrated, and the interesting finds were again not styling.** Page + `CustomerTable`: 21 raw-palette classes and every hard-coded hex → **0**; hand-rolled `<table>`/`<input>`/`<select>` → `DataTable`/`Input`/`Select`; `max-w-7xl` → `PageShell`. **Three dead buttons removed rather than restyled** — "Search" (no handler, and the filter is already live as you type, so it implied results were stale until clicked, which was never true), "More filters" (no handler, no filters behind it) and "Export" (no handler). A control that looks actionable and does nothing is worse than an absent one; the user blames themselves. Export is a genuine need and belongs on `DataTable` behind a real endpoint. **Two real contrast failures fixed:** segment badges rendered `text-emerald-400`/`blue-400`/`amber-400`/`slate-400` on white (≈2.5:1, well under AA) — now the `Badge` primitive; and the avatar palette contained `#EAB308`, which is **1.9:1** against the white initials drawn on it, i.e. effectively unreadable — replaced with six identity colours all ≥4.5:1 (documented as deliberately non-semantic: they carry no meaning, they only need to be distinguishable). **Primitive extended, not forked:** `DataTable.expandedContent` so the expand-in-place detail panel survived the migration — the whole point of this view is comparing customers, and navigating away loses scroll position and filters. Gates: typecheck, lint, build (123/123), **226/226 tests** (+3 expansion tests), hygiene — all green. **Remaining Phase 3:** dashboard, terminal, catalog, inventory, purchasing, receiving. Still no browser/visual QA on any of it. |
| — | 2026-08-10T184500Z | (ui-phase3-orders) | **UI/UX Phase 3 begins — `/orders` migrated end-to-end, and it surfaced two real defects that had nothing to do with styling.** Page: 44 raw-palette classes → **0**, hand-rolled `<table>` → `DataTable`, `max-w-7xl` → `PageShell`, 409 → **249 lines**. Rebuilt around the actual workflow (server-side status filter as toggle buttons with `aria-pressed`, not a tablist — there is no tabpanel; money right-aligned and tabular; order number sticky + mono so it survives horizontal scroll). **Defect 1, dead duplicate business logic:** the page carried an `OrderDetailModal` plus refund and void handlers that **could never open** — `setSelectedOrder` was only ever called with `null` while row clicks navigated to `/orders/[id]`. Two money-moving operations therefore existed twice in the tree, unreachable and free to drift from the live implementation on the detail page, which has confirm dialogs and toasts where the dead copy had bare `alert()`. The file's doc comment asserted the inline flow worked. Removed, comment corrected — `AGENTS.md` calls this the costliest failure class. **Defect 2, found by a test I wrote against my own new code:** `DataTable`'s `Previous` button was disabled from `Math.floor(offset / limit) === 0`, so any offset that is not a multiple of the page size (deep link, changed page size, rows deleted between loads) floored to page 0 and left **every earlier row unreachable**. Pagination now derives from the offset directly, never a page index. **Primitives extended rather than forked:** `DataTable.serverPagination` (real enterprise lists page on the server; client-side search/sort deliberately left off here because they would silently act on the loaded 25 rows and read as if they had searched every order) and `PageShell.titleAs` (EnterpriseShell already emits an `sr-only` h1, so a PageShell h1 produced **two h1s per page** — the pre-existing markup was worse than what replaced it). Gates: web typecheck, lint, production build (123/123 routes), **223/223 tests** (+5 server-paging tests), hygiene — all green. **Remaining Phase 3:** dashboard, terminal, catalog, inventory, purchasing, receiving, customers. No browser/visual QA has been run on any of it. |
| — | 2026-08-10T172828Z | (sync-develop-staging) | **`develop` and `staging` re-synced — the drift was in the history graph, not the trees.** `git diff origin/develop origin/staging` was only 2 files (`WORK/LOCK.md`, `docs/architecture/PIPELINE.md` — PR #209's record of *why* `master` cannot accept a merge, which had landed on `develop` and never been promoted). But the graph told a different story: `origin/master` was **not an ancestor of `develop`** (57 commits) and 66 `staging` commits were missing from it, so `AGENTS.md`'s `develop ≥ staging ≥ master` invariant was structurally broken even though the code was aligned. Cause: those 66 commits are pure ancestry — `git diff <merge-base> origin/staging` is **empty** — so `develop` had all the *content* while `staging` alone carried the *lineage* back through `master`. Fixed by back-merging `origin/staging` into `develop` (merge `d58c7e5`): no conflicts, and the result's tree hash is byte-identical to `origin/develop`'s (`dd9150d3`), so the merge provably changes zero files. After it: `master` and `staging` are both ancestors of `develop`, `develop` is 3 ahead / **0 behind** `staging` and 187 ahead / **0 behind** `master`. **Second half still open:** `staging` does not yet have PR #209's two files — that needs the ordinary `develop → staging` promotion PR, which must merge *after* the back-merge or it re-opens the graph split. `master` remains unmergeable for the reason PR #209 documented (branch protection requires the status check `Frontend — typecheck + lint + build`, renamed to `… + test + build` by `1a4b989` on 2026-08-05 and never updated) — **Sri-only, unchanged by this work.** |
| — | 2026-08-06T170650Z | (erp-infrastructure-audit) | **12-phase infrastructure/deployment/integrations/tooling audit.** Full report: `WORK/audits/AUDIT_2026-08-06T170650Z-erp-infrastructure-audit.md`. Overall readiness **61/100** — the distribution is the finding: ~86 avg on architecture/maintainability/ERP, ~35 avg on observability/deployment/reliability. **Backlog F-2 closed:** the `guard` job's "No unguarded mutation routes" step ended `! grep … \|\| echo "…✓"`, which laundered a match into exit 0 — reproduced locally printing **39 matching lines AND the ✓ AND exit 0**, so it had never once evaluated this codebase. Its detection was also wrong both ways (excluded only literal `requireRole`, but 44 route files alias it as `mgr`; `-A1` missed middleware on later lines). Replaced with `tools/route-guard-scan.mjs`, which parses the real argument list: **314 mutating routes / 49 files / 76 unguarded**, each classified in a shrink-only allowlist as `open-by-design:` (62, correct — POS/floor/self-service/public auth), `in-handler:` or **`GAP:` (14, real authorization debt** — `catalog POST /` lets a cashier create products, `billing POST /bills`+`/invoices` let a cashier create AP/AR documents while the *pay* route is guarded, `quotes DELETE /:id` is destructive and that module imports no guard at all). Proven to fail on a new unguarded route, pass on a guarded one, and fail on a stale allowlist entry. **Also fixed:** `JOBS_TICK_SECRET` was documented in `.env.example` since the endpoint existed and read **nowhere** in `src/` — an operator on a non-Vercel host who followed the docs got `503 cron_unconfigured` and **no background jobs at all**, silently (ADR-012, +2 tests); `/metrics` and `/jobs/tick` now compare secrets in constant time; the production `smoke-test` job read a hardcoded `ascendhq-api.vercel.app` (dead since 2026-07-23) while `uptime.yml` was already repointable — both now read the same repo variables so they cannot disagree about where production is (ADR-011). **Added:** gating secret scan (`gitleaks` — first full scan found 4 hits, **all false positives**, so it ships able to fail, verified against planted AWS/Stripe/GitHub keys), CycloneDX SBOM + licence inventory (858 components, **0 copyleft**, 2 MPL-2.0, 2 undeclared), dependency-advisory reporting, container `HEALTHCHECK`, `SECURITY.md`, ADR-010/009/010. **NEEDS-SRI, unchanged and still the top risk: `PROD_DATABASE_URL` is unset, so no production backup has ever run.** Deliberately not changed (each with a reason in the audit's §12.6): the ungated routes, `compile()`'s silent-NULL binding, the dead `errorEnvelopeMiddleware`, `vercel.json`'s daily cron, the Dockerfile's `npm install`, `render.yaml`, and the legacy `finder-pos` CORS origins. **DUPLICATE-WORK COLLISION (the costliest kind, per `AGENTS.md`):** PR #198 ran a near-identical enterprise platform audit in the same week and merged first. Both sessions independently built a route-authz scanner, a `security.yml`, a `SECURITY.md`, a Dockerfile `HEALTHCHECK`, and ADRs numbered 008/009. Resolved on merge by adopting #198 wherever it overlapped — its scanner (`route-authz-scan.mjs`), its `/readyz` HEALTHCHECK (better than this PR's `/healthz` one: `/healthz` answers 200 from a process that cannot reach Postgres), its `SECURITY.md`, its quotes `DELETE` fix — and keeping only what was genuinely additive: the gitleaks secret scan, CycloneDX SBOM and licence inventory (the two `security.yml` files turned out to be complementary, CodeQL + dependency-review vs secrets + SBOM, with zero job overlap), the repo-variable probe targets, `JOBS_TICK_SECRET`, and constant-time secret comparison. This PR's ADRs renumbered 008/009/010 → **010/011/012**. Neither session claimed the work in `WORK/LOCK.md` before starting, which is exactly the check that would have prevented it. |
| — | 2026-08-06T165353Z | (progress-loop) | **FEATURE, delivered end-to-end: the progress-intelligence loop is no longer half-shipped.** `AGENTS.md`'s mandated `Hypothesis → Plan → Task → Evidence → Verified Result → Decision` model had real, tested, audited backend routes for hypotheses and decisions and **zero frontend** — `grep -rni "/decisions" web/` returned nothing — while evidence was write-only because **no `GET` for evidence existed at all**. Added 3 bounded, tenant-scoped read routes (`GET /hypotheses/:id` returning the whole loop in one read, `GET /evidence?taskId=\|hypothesisId=`, `GET /hypotheses/:id/decisions`), a new `/progress` page + 3 components + `web/lib/progress.ts`, RBAC feature id, nav entry, and full MSW parity. **Two correctness fixes found while building:** `listHypotheses` was unbounded (the movements/audit_log/journal defect class, 4th occurrence) → `clampLimit`; and the "what counts as evidence for this hypothesis" predicate was written twice, so a read could have disagreed with the decision gate → extracted to one shared `EVIDENCE_FOR_HYPOTHESIS` constant used by both. Gates: backend 894/894, web 206/206 (29 files), 18 new web tests, 4 new backend tests, typecheck/lint/production-build/hygiene/gap:scan/table:scan all green. `dashboard/**` deliberately untouched (live Cursor lock). Full report: AUDIT_2026-08-06T165353Z-progress-intelligence-loop.md |
| — | 2026-08-04T040621Z | (ai-slop-audit) | **CI was red on `develop`: the npm root was a foreign workspace stub again** (`a4dbf2c` — `name:"workspace"`, `preinstall` rejecting npm + deleting the lockfile, `package-lock.json` gone, `tsconfig.json` a `files:[]` stub, 4 env templates deleted). All 7 `npm ci` steps could not run. Restored the root from `ca7ec4b`. **Count correction:** this audit called it the 3rd occurrence, which was all the tree at `a4dbf2c` could show — `AUDIT_2026-08-03T1904/1907Z` were not yet committed there. It was the **5th**; PR #182 restored the root independently and my restore was superseded on merge (kept: the guard, which #182 explicitly says it does not provide). Built the guardrail that audit recommended and skipped: `hygiene-check.mjs` check 8 asserts root name/scripts/no-npm-rejecting-preinstall/lockfiles/tsconfig-compiles-src, wired as CI's FIRST step as a bare `node` call so it survives a root with no scripts — verified it emits all 6 violations against the real broken commit. Also: `.gitignore`'s bare `.env*` (no `!.env*.example`) had made the template deletion **irreversible**; a private `apiFetch` fork in `StoreAuthContext` showed users `[object Object]` on failed storefront sign-in and read a nonexistent env var — consolidated onto the shared client with a regression test proven to fail against the old code; 4 dead `gap:scan` allowlist entries removed + scanner no longer counts doc-comment paths as live calls. **NEEDS-SRI: `artifacts/` is a 1,004-file bidirectionally-diverged copy of the whole app — a finished, tested `push_tokens` module lives only there and ships to nobody.** Full report: AUDIT_2026-08-04T040621Z-ai-slop-consistency-audit.md. |
| — | 2026-08-03T060225Z | (wave3) | Ponytail Wave 3 on develop: deleted `/reporting/*` + every thin alias page twin (next.config redirects are the sole SoT); Outlets ownership inverted to `/setup/outlets` with `/operations` redirecting; Pricing quarantined to Customer Overrides behind SHOW_PARTIAL; Delivery stage chrome retokenised. Waves 0–2 already on develop via PR #160 — deliberately not replayed (parallel-session collision, recorded in the audit). Root `tsconfig.json` no-op typecheck found here too, but left to its dedicated hotfix PR #171 rather than fixed twice. |
| — | 2026-08-03T054700Z | (ui-audit) | Continue product-experience Wave A/B: #165 closed superseded (develop already restored); #163 Quick Sell deep-link; #166 outlet filter scope; #169 dead-chrome + Quick Sell href. Waiting CI then merge. |
| — | 2026-08-03T042850Z | (wave1) | Ponytail Wave 1: reporting/sell/finance/setup redirects; Inventory nav trim + Purchasing hub ?tab=; Delivery→Sell; checklist/dashboard link fixes. |
| — | 2026-08-02T232920Z | (wave0) | Ponytail Wave 0 honesty: hide Error Center + Kiosk (partial); /sales→/orders; Pipeline mock tabs gated; brand F→A + finder-pos URLs; kiosk no fake save. |
| 1 | 2026-07-15T17:30Z | 3665437 | movements route drift (mock-only → real, prod panels were blank) + keyset pagination on inventory movements + audit_log cursor mode; 27/27, smoke 20/20 |
| 2 | 2026-07-15T17:40Z | c6eb35b | loop durability infra: LOOP_PROTOCOL.md (on-disk program, re-read each wake) + LOOP_STATE.md (heartbeat/backlog/counters) + cloud-watchdog contract + memory pointer |
| 3 | 2026-07-16T03:28Z | e535d7f | cloud watchdog live (trig_01VVXryUgSBHoy9mAqRdhfzz, notify-only, Gmail); protocol watchdog contract revised do-work→notify-only for financial-repo safety |
| 4 | 2026-07-16T03:47Z | 867ead1 | route drift: customer-invoices/service-orders/product-batches mounted at underscore path → 404 in prod (mock-masked); added mountPath /api/v1 (store_locations convention) + mount test; removed 51 gitignored ` 2.` dupes blocking local tsc; 2/2 + smoke 20/20 |
| 5 | 2026-07-16T04:05Z | 09a0083 | ledger pagination: accounting.listJournal was bare LIMIT 500 on journal_entries (most append-heavy table) → deep audit history unreachable; added keyset cursor (additive {items,nextCursor,limit}); reports verified already-bounded aggregations; 19/19 + smoke 20/20 |
| 6 | 2026-07-16T04:25Z | 6ae6bb5 | sync authz gap: /online /push /pull /integrations had NO role guard (any cashier could toggle company sync / drain queue / connect integrations); added requireRole manager (ops) + owner (integrations, matches webhooks); webhooks verified already-guarded; 9/9 + smoke 20/20 |
| — | 2026-07-16T05:16Z | 29a27d7 | **PR #66 MERGED to master + deployed to prod** — /readyz db:connected under C-3 cert verification; 6 loop fixes live |
| 7 | 2026-07-16T05:45Z | 89b2e3b | module-wide authz sweep: reports POST /ar-aging/sweep (AR dunning mutation) + ecommerce PUT /products/:id/online (storefront publish) were unguarded → requireRole(manager); team verified guarded (in-handler requireManagement), orders/payments POS-by-design; 2 new 403 tests, reports 11/11 + ecommerce 9/9 + smoke 20/20 |
| 8 | 2026-07-16T06:00Z | 7ce3e6e | authz sweep completed: notifications POST / was unguarded (cashier could post spoofed notifications) → requireRole(manager); internal event-driven creation bypasses route (proven); first tests for the module; 2/2 + smoke 20/20. 3 sweeps (drift/pagination/authz) now exhausted — loop winding down |
| 9 | 2026-07-16T06:15Z | b89ae6b | tenant-scoping sweep across all service queries: VERIFIED CLEAN — no cross-tenant leaks (verify-then-mutate pattern is consistent; dynamic where-builders include tenant_id; RLS backstop). No fix needed. 4th sweep — loop stopped, then Sri redirected to INVENTORY |
| 10 | 2026-07-16T06:30Z | e3523b5 | INVENTORY: stock-adjust oversell race — adjust() was read-modify-write, concurrent sales lost updates (10 −6 −6 → 4 not 0). Added FOR UPDATE (matches FEFO path) + ON CONFLICT upsert. Deterministic concurrency test (2nd connection, lock barrier) — VERIFIED fails without fix. 25/25 inventory + smoke 20/20 |
| 11 | 2026-07-16T11:50Z | caedd71 | INVENTORY: transfer atomicity — createTransfer moved stock via 3 independent statements (2 separate adjustStock txns + INSERT); failure between legs lost stock. Extracted adjustStockTx(tdb) (+FOR UPDATE), wrapped whole transfer in ONE tx. Atomicity test (INT_MAX overflow forces 2nd-leg failure) — VERIFIED fails without fix (source lost 5). 27/27 + smoke 20/20 |
| 12 | 2026-07-16T12:10Z | e6fe4f4 | INVENTORY: cycle-count double-close — closeCycleCount checked open→applied variances→closed non-atomically; 2 concurrent closes double-posted variance (−3 → −6). Extracted adjustTx(tdb) from adjust(), wrapped close in ONE tx with session FOR UPDATE (2nd close 409s). Deterministic barrier test (3rd conn holds inventory lock) — VERIFIED fails without fix (double-posted → 4). 29/29 + smoke 20/20 |
| 13 | 2026-07-16T12:40Z | (this) | INVENTORY: transfer over-draw phantom stock — createTransfer never checked source on-hand; adjustStockTx clamps source debit at 0 but dest gets full credit → 100 from a loc with 10 conjures 90 units. Added source FOR UPDATE + availability check (409 insufficient_stock). Over-transfer test VERIFIED fails without guard. 30/30 + smoke 20/20 |
| — | 2026-07-18T233000Z | aaddd67 | LOOP RESTARTED (Sri directive): Phase 0 added to FORWARD_PLAN.md as top priority, loop_status ACTIVE, counters reset. Session G claims Phase 0 coordinator role in LOCK.md |
| 14 | 2026-07-19T003000Z | 0e83ce9 | Phase 0 wave 1a: notifications digest/preferences/rules built as real backend (dispatched to a subagent, verified by coordinator before/after: typecheck clean, 12/12 new tests + 2/2 pre-existing regression, gap:scan 30 allowlisted (was 34)) |
| 15 | 2026-07-19T010000Z | 6db150d | Phase 0 wave 1b: purchasing EDI-imports + vendor-history built (subagent). vendor-history is a real join (zero blockers); EDI validate/process are honest state-machine transitions, not real parsing — frontend never uploads file bytes (NEEDS-SRI added). 16/16 new tests + 19+/22 pre-existing purchasing regression, gap:scan 24 allowlisted (was 30) |
| 16 | 2026-07-19T013000Z | c65df4d | Phase 0 wave 1c: workflows approval-chains + run-history built (subagent), new tables distinct from workflow_definitions. runs is a real COUNT over a real invocation log; nothing invokes it yet (NEEDS-SRI added — no POS/refund/discount action checks a chain today). 13/13 new tests + 11/11 pre-existing workflows regression, gap:scan 21 allowlisted (was 24) |
| — | 2026-07-19T0528Z–0535Z | 77937a3, b859ec6, 5b20519 | Follow-on fixes ahead of this iteration's verification pass: ReorderTab contract-drift fix (missing last-ordered fields), MFA/device-verification claim corrected + Preview-labeled, RLS backstop proof extended to a table created this Phase-0 effort — see commit messages for detail |
| 17 | 2026-07-19T062148Z | (this — docs/board commit) | **Phase 0 closing verification pass.** Ran every stage of `npm run verify` to completion across ~35 batched tool calls (sandbox can't sustain one long call): hygiene PASS, gap:scan PASS (444 backend/373 frontend paths, 21 allowlisted), backend typecheck PASS, **601/601 backend tests passing across all 78 test files** (one batch showed 13 failures traced to the sandbox's tmp volume filling with ~9GB of orphaned embedded-Postgres dirs from timeout-killed runs — cleaned up, reran clean, confirmed environmental not a code bug), smoke 20/20 steps PASS, frontend typecheck PASS, frontend lint PASS (0 errors, 4 pre-existing warnings). `web build` could NOT be completed in this sandbox — confirmed architectural limit (background processes don't survive across tool calls, proven with a plain `sleep` test), not a code/config issue. No genuine application bugs found this pass — a clean confirmation, not a fix session. Wrote `AUDIT_2026-07-19T062148Z-phase0-verification.md` with a full 51-module status table. Updated FORWARD_PLAN.md Phase 0 exit criteria (4 of 5 fully met with evidence, 5th partially met — everything but the unconfirmed `web build`) |
| 18 | 2026-07-19T145000Z | 895aedd (+ fix bb15942) | Test coverage: quotes + loyalty (subagent, coordinator-verified). **Real bug found+fixed:** quotes module's `quotations` table collided with sales' pre-existing incompatible `quotations` table — sales registers first in modules/index.ts so its schema always won, meaning **every quotes-module insert 500'd — the module was 100% non-functional**. Renamed to `customer_quotations`/`customer_quotation_lines`. Also fixed a dead `already_converted` guard reading a never-populated column instead of checking `status`. 26 new tests (12 quotes + 14 loyalty), all independently re-run and confirmed passing by the coordinator; typecheck/gap:scan reconfirmed clean |
| 19 | 2026-07-19T151500Z | 187adb4 (+ fixes 0d191de, 800c43e) | Test coverage: store_locations, product_batches, service_orders, customer_invoices (subagent, coordinator-verified). **Two more real bugs found+fixed:** (1) store_locations' `product_locations` table collided with fulfillment's pre-existing incompatible table of the same name (same bug class as #18, 3rd occurrence this effort) — renamed to `store_location_products`; every product-location write 500'd. (2) customer_invoices' `create()` used SQL placeholders (`@subtotal` etc.) that didn't match the actual params object keys (`subtotal_cents` etc.) — `shared/db.ts` silently binds NULL for unmatched `@name` placeholders, so **every invoice creation with any lines 500'd on a NOT NULL violation** (the module's primary write path); also fixed a status-update route bypassing `parseBody()` and leaking raw 500s instead of 400s on bad input. 61 new tests across the 4 modules, all independently re-run and confirmed passing (24+26+9 regression); typecheck/gap:scan reconfirmed clean |
| 20 | 2026-07-19T153400Z | 8e34f41 (+ fix af1f549) | Test coverage: workforce (subagent, coordinator-verified) — last module in the audit's §3/§4 list. Confirmed the earlier `time_entries`/`team_time_entries` collision (fixed 2026-07-18) has not regressed, and grepped every module's `CREATE TABLE` for new collisions — none found. **One real bug found+fixed:** `clockIn()` never validated `employeeId` against the employees table (unlike `createShift()`/`createTimeOff()`, which both do) — a bogus id returned 201 and created a permanently-invisible orphaned `time_entries` row (JOIN-filtered out of every list) while also permanently occupying that id's "already clocked in" slot, an unrecoverable stuck state. Fixed with the same existence check used by sibling creators. workforce.test.ts expanded from 1 to 27 tests, independently re-run and confirmed passing; typecheck/gap:scan reconfirmed clean. **This closes the entire test-coverage gap named in the Phase 0 verification audit — 4 real bugs found this wave, 8 total for the whole Phase 0 effort, purely from writing tests for "Built but not verified" modules.** |
| — | 2026-07-20 → 2026-07-30 | many, not loop-tracked | **Consolidated summary — not backfilled iteration-by-iteration (this work ran as direct Sri-directed sessions, not loop selections).** Full detail lives in `WORK/LOCK.md` and `WORK/audits/`, not here. Major waves: (1) Phase 6 — procurement/reorder intelligence (MOQ/pack-size rounding, safety stock, promised delivery date) + an `insights.createReorderPOs()` bugfix, `AUDIT_2026-07-28T194619Z`. (2) Phase 7 item 1 — sales-velocity consolidation across 5 previously-divergent reorder-suggestion call sites, fixing 2 independent real bugs in the process, `AUDIT_2026-07-28T210834Z`. (3) Phase 7 item 2 — demand-snapshot foundation module (PR #121, open). (4) UOM v1.0 + POS v1 — purchasing/receiving unit conversion (ADR-006) + POS barcode resolution. (5) `ai_assistant` module (ADR-005) finished + verified from an uncommitted working-tree state. (6) Production-heartbeat investigation — root-caused, corrected, then finally merged after sitting unmerged a week (PR #117, commit `978c154`); full incident record in `docs/architecture/DEPLOYMENTS.md` (new 2026-07-30, supersedes the "heartbeat gone silent" NEEDS-SRI item below — it isn't silent, it fires every ~15min and fails on a now-understood cause). (7) Four-environment AI coordination workflow adopted 2026-07-30 — two-tier claim model (GitHub Issues + short-lived `LOCK.md` locks), bootstrap pointers for Cursor/Replit (`.cursor/rules/ascend.mdc`, `REPLIT.md`, restored `CLAUDE.md`), `docs/architecture/ORCHESTRATION.md`'s "Environment routing" section, `tools/dashboard.mjs` visibility dashboard, PR template + ADR template. (8) 4 long-stale `WORK/LOCK.md` claims (sessions B/C/D/G) reviewed and closed with evidence (PR #126) — 2 confirmed shipped, 1 confirmed abandoned (branch deleted, never merged), 1 superseded. |

## Backlog (loop-selectable, in priority order)

| Item | Status | Evidence / notes |
|---|---|---|
| Mock-vs-real drift sweep | DONE (iter 4, AUDIT_2026-07-16T034500Z) | Fixed 3 real 404s (customer-invoices/service-orders/product-batches → mountPath /api/v1). Remaining 16-candidate mismatches all benign: golf/pricing/warehouse/documents/promotions are Preview verticals (UI-only by design, NOT bugs per ae79907); audit-log/custom-roles have hyphenated name fields; store/product-locations served by store_locations mountPath; `things` is a JSDoc example |
| SSO/identity token-issuance consolidation (sso duplicates issueLoginSession insert) | BLOCKED — session C claim on sso/index.ts still ACTIVE | AUDIT_2026-07-15T163000Z note |
| Ledger/accounting unbounded list check | DONE (iter 5, AUDIT_2026-07-16T040500Z) | journal_entries listJournal → keyset cursor; reports verified already-bounded (GROUP BY/top-N aggregations, not row lists) |
| requirePermission granularity on sync/webhook mutation routes | DONE (iter 6, AUDIT_2026-07-16T042500Z) | sync mutations were UNGUARDED → added requireRole manager/owner; webhooks verified already owner-guarded (no gap) |
| Web client adoption of error.details for field-level form errors | DONE (2026-07-19 iter 23 — api-client parses `error.details` into `ApiResponseError.details` (runtime-guarded), new `fieldErrors()`/`errorMessage()` helpers exported from the barrel, and NewCustomerModal wired as the reference adoption (per-field inline messages keyed by API payload field names, "(root)" issues fall back to the flattened banner). Remaining forms adopt the same three-line pattern incrementally. web typecheck 0, lint clean, gap:scan clean | shared/http.ts details added fd2dd2a |
| PROJECT_STATUS.md stale internal refs cleanup | MOOT (confirmed 2026-07-30) | File no longer exists at the repo root — already folded into the 2026-07-19/20 doc consolidation along with everything else `AGENTS.md` lists as archived. Nothing to clean up. |
| Catalog product-detail endpoints (16 mock-only paths: analytics, audit-log, pricing/tiers, suppliers, stock, sales, returns, …) | DONE (2026-07-18 2nd follow-up — 17 of 18 built: real joins for stock/sales/sales-by-customer/purchases/invoices/returns/duplicate; derived analytics/reorder-suggestions/supplier-price-comparison; new CRUD for suppliers/pricing+tiers/expiry/images; audit trail instrumented + GET audit-log. New tables: product_suppliers, product_price_tiers, map_price_cents, extended inventory_lots. 19 tests, all passing. Only credits remains (no backing concept — see NEEDS-SRI) | AUDIT_2026-07-18T005030Z addendum #2 |
| Team time-tracking endpoints (clock-in/out, time-entries, permission-overrides/requests) | DONE (2026-07-18 follow-up session — team_time_entries table [renamed from time_entries after a collision with workforce's existing table of that name, fixed same session], atomic clock in/out, self-or-mgmt guard; tests written AND now verified passing) | AUDIT_2026-07-18T005030Z addendum |
| Customers /search + /:id/merge; orders /:id/timeline | DONE (2026-07-18 — transactional merge w/ sorted locks + customer.merged event; derived timeline; tests written AND now verified passing) | AUDIT_2026-07-18T005030Z addendum |
| settings/custom-roles FE↔BE path mismatch (backend at /api/v1/custom-roles) | RECLASSIFIED → NEEDS-SRI: contract mismatch (color+feature-keys vs permissions vocab), not a path fix | AUDIT_2026-07-18T005030Z addendum §7 |
| Notifications digest/preferences/rules | DONE (2026-07-18 — real backend, digest/preferences read sensible defaults on a missing row, alert rules are real CRUD) | commit 0e83ce9 |
| Purchasing EDI-imports + vendor-history | DONE (2026-07-19 — formats/list/detail/validate/process built as a real status-tracked table (edi_imports); vendor-history is a real join over purchase_orders grouped by supplier_id. validate()/process() are honest state-machine transitions, NOT real EDI parsing — the frontend upload form never sends file bytes (see edi-imports.ts doc comment + NEEDS-SRI below). 16 tests, all passing) | this session |
| Workflows approval-chains/run-history | DONE (2026-07-19 — approval_chains is real CRUD (owner-gated mutations, manager-read) with an ordered steps[] list and a real COALESCE-COUNT `runs` from an append-only approval_chain_runs invocation log; run-history is a new, real, keyset-paginated workflow_run_history table (shared/pagination.ts convention). Both new concepts, new tables — distinct from workflow_definitions/workflow_steps. Neither table has real invocations yet: no code path (POS price override, refund, vendor create, discount create) currently checks against a chain or logs a run — see NEEDS-SRI below. recordRun() exists on both services as ready-to-call plumbing. 13 tests, all passing) | this session |
| Inventory pipeline pending/history/reorder-alerts | DONE (2026-07-18 3rd follow-up — new pipeline-views.ts/pipeline-routes.ts, real joins over purchase_orders/lines/suppliers/products; reorder-alerts extends the now-fixed getReorderSuggestions with velocity/stockout/cost fields + create-po action. 4 tests, all passing) | AUDIT_2026-07-18T005030Z addendum #3 |
| Inventory pipeline receiving/issues/errors/summary | RECLASSIFIED → NEEDS-SRI: each implies an unbuilt subsystem (receiving sessions, issue/error detection engine, a stage funnel that doesn't match POStatus) — same class as catalog credits | AUDIT_2026-07-18T005030Z addendum #3 |
| API gap-scan guardrail | DONE (2026-07-18 — tools/api-gap-scan.mjs in CI hygiene job + npm run verify; allowlist board-tied, shrink-only; now down to 34 entries, was 56. Hardened same day to also catch API-client calls missing the /api/v1 prefix entirely — a distinct blind spot from the missing-route check) | AUDIT_2026-07-18T005030Z addendum §6, addendum #3 |
| Three live bugs found while surveying inventory pipeline (unrelated to the survey itself) | DONE (2026-07-18 3rd follow-up): (1) getReorderSuggestions + serial_numbers joined a nonexistent `catalog_products` table/columns → live 500 on 2 shipped pages, fixed to join real `products`/`product_suppliers`; (2) inventory routes.ts GET /:productId (registered early) silently shadowed GET /counts, /locations, /reorder-suggestions registered after it — moved ahead of the catch-all; (3) inventory/serials page called the API with no /api/v1 prefix at all + serial_numbers module had no mountPath, AND simply adding one collided with inventory's own /:productId catch-all — fixed by reordering serialNumbersModule before inventoryModule in modules/index.ts. Also fixed the PO-status/column bug in catalog's reorderSuggestions (`'partial'` typo, `billed_qty` vs `received_qty`) from the prior session's leftover. All with regression tests | AUDIT_2026-07-18T005030Z addendum #3 |

| **NEW 2026-08-07 — the backend suite's 30s statement timeout covers an unbounded migration-lock WAIT (flake source)** | `buildApp()` runs migrations inside `db.tx()`, and `db.tx()` issues `SET LOCAL statement_timeout = <30s>` (`src/shared/db.ts:124`). The transaction's **first** statement is the *blocking* `SELECT pg_advisory_xact_lock(7381920)` (`src/app.ts:212`) — so time spent **queuing** for the lock is charged against the same 30s budget as real work. **123 call sites across 86 test files** each build a fresh schema, so every one serializes on that single global lock. On a slow runner this fails as a bogus "statement timeout" in a test that did nothing wrong. Observed live: CI run 31138020800 attempt 1 failed **893/894**, the loser being `settings/settings.test.ts` "get and update feature flags" at exactly 30014ms with pg code `57014`, while the Postgres service container was checkpointing at `write=70–140s` / ~195k file syncs. Attempt 2 on a healthy runner passed **894/894 + smoke**. Fix options, cheapest first: take the advisory lock **outside** the timeout-bounded transaction (or `SET LOCAL statement_timeout = 0` for just the lock statement), or use `pg_try_advisory_xact_lock` in a bounded retry loop so the wait is explicit rather than disguised as a query timeout. Not fixed in PR #196 — one concern per PR, and it is test-harness infrastructure, not the feature. |
| **NEW 2026-08-06 — no e2e spec can see a manager-gated control (whole-suite blind spot)** | `web/lib/auth.ts` persists the user profile to **sessionStorage**; Playwright's `storageState` captures cookies + localStorage only. So every spec that reuses `e2e/.auth/owner.json` starts with `getUser()` null → `hasRole("manager")` false → **every manager-gated control is hidden**. Proven, not inferred: a probe against the running stack found the shipped dashboard `ProgressPanel`'s "New task" form at `count=0` with `sessionStorage` keys `[]`, exactly like the new progress form. **Consequence: the e2e suite has never exercised a create/edit/approve control**, only read-only surfaces. `e2e/progress.spec.ts` works around it locally by logging in through the form (`test.use({storageState:{cookies:[],origins:[]}})`), which runs `setSession`. The real fix is a decision: either persist the profile somewhere `storageState` captures, or make `global.setup.ts` produce a state that survives. Until then, treat "e2e passed" as covering reads only. |
| **NEW 2026-08-06 — `inventory-receive.spec.ts` PO-detail test is deterministically broken** | `e2e/inventory-receive.spec.ts:38` calls `.count()` on the row locator to branch. `count()` does **not** auto-wait in Playwright, so it reads 0 before the PO list's fetch resolves, takes the "no POs" branch, and then waits 8s for empty-state text that never appears because the table has meanwhile rendered. The failure screenshot proves the page worked — it shows the seeded `po_e2e_001 / E2E Coffee Supply Co / ordered / $124.90` row. Fails identically on retry, so it is deterministic, not flaky. Fix is to await the rows (`await expect(firstPO).toBeVisible()` or `page.getByRole("row").nth(1).waitFor()`) before branching. **Not fixed in PR #196** — one concern per PR (Phase 9.2c), and e2e only runs on `master`/`staging` pushes so nothing is blocked today. |
| **NEW 2026-08-06 — `ProgressPanel` duplicates the shared progress vocabulary** | `web/app/(protected)/dashboard/_components/ProgressPanel.tsx` carries private `STATUS_LABEL` / `STATUS_BADGE` / `VERIFICATION_SOURCES` copies that now duplicate the canonical `web/lib/progress.ts`. It should import the shared module and drop its own. **Deliberately not done in the same change:** `dashboard/**` sits inside the Cursor Cloud "Wave A/B trust leftovers + palette" claim, still `ACTIVE` in `WORK/LOCK.md`. Pick this up once that lock releases — it is a delete-and-import, no behaviour change. Same F-4/F-5 duplication class Phase 9 is burning down. Detail: `AUDIT_2026-08-06T165353Z-progress-intelligence-loop.md` §6.2 |
| **RESOLVED 2026-08-06 — F-2 (unguarded mutation routes) closed; F-1 was already closed by `c00a485`** | Replaced the inert grep step with `tools/route-authz-scan.mjs` (`npm run authz:scan`, also in `npm run verify` and CI's `guard` job). Repairing the `\|\| echo` alone would not have been enough — the grep only matched the literal `requireRole` on the route's own line, so the 20+ files using the hoisted `const mgr = requireRole("manager")` convention all read as unguarded; as written it reported **39 findings, ~35 false**. The scanner resolves aliases, honours `router.use(guard)`, and recognises all five authz helpers. It reports **4 real findings**: `quotes DELETE /:id` was **fixed in code** (manager-gated + regression test — a cashier could hard-delete a customer quotation with no soft-delete column and no audit entry), and 3 were reviewed and allowlisted with reasons. Reasoning: `docs/architecture/ADR/ADR-008`. |
| **NEW 2026-08-06 — enterprise infrastructure/platform audit** | Full 12-phase audit: `WORK/audits/AUDIT_2026-08-06T170227Z-enterprise-platform-audit.md`. Overall production readiness **64/100** — Architecture 88 / Security 82 / DX 85 / Testing 78 **against** Deployment 35 / Compliance 40 / Reliability 45 / Observability 48. **Every serious finding is operational, not architectural.** Seven backward-compatible changes shipped (authz scanner + quotes fix, `/metrics` pool/queue/outbox/runtime gauges, CodeQL + dependency review, root `npm audit` promoted to a `high` gate, honest `backup.yml` annotations, Dockerfile `HEALTHCHECK`, `SECURITY.md`). Also added ADR-008/ADR-009 and new `GAPS.md` rows. Baseline measured this session: backend typecheck/hygiene/gap:scan/table:scan PASS, **893/893 backend tests on real Postgres 16**, 0 fail (3 of them added here), web **188/188** + lint + build PASS, root `npm audit` **0 advisories**, web **10** (1 critical, 6 high). |
| **NEW 2026-08-06 — ungated POST mutations are covered by no CI check** | `route-authz-scan` deliberately checks only `PUT`/`PATCH`/`DELETE` (sound: in a POS a `POST` is usually the normal cashier action). A parallel sweep in PR #197 that also covered `POST` found 314 mutating routes, 76 with no authorization middleware — 62 correctly open, and a residue no scanner watches: `catalog POST /` (a cashier can create a product), `catalog POST /:id/barcodes`, `catalog PUT /:id/categories`, `billing POST /bills` + `POST /invoices` (a cashier can raise AP/AR documents while the sibling *pay* route **is** guarded), `outlets POST /` + `POST /:outletId/registers`, and `inventory POST /pipeline/reorder-alerts/:id/create-po`. Each needs a product decision about who may do it. Two ways to close: extend the scanner to POST with a per-route allowlist, or gate them individually. Detail: `GAPS.md`, `AUDIT_2026-08-06T170650Z-erp-infrastructure-audit.md` §4 F-2. |

## NEEDS-SRI (out of loop scope — decisions/actions only Sri can take)

**Added 2026-08-10 — the five that gate production actually working.** These came out of a
`master` vs `staging` vs `develop` status review and are the whole difference between "the release
merged" and "the release works". Full step-by-step prompts for each are on the session task board;
the one-line versions are here because this file is the single live work-updates record.

| Item | What's needed |
|---|---|
| **Render: confirm production answers, then leave the Free plan** | `DEPLOYMENTS.md`'s open P0. Load `https://ascend-prod.onrender.com/healthz` in a browser and **wait a full minute** — every probe that concluded production was dead used a 15s timeout against a Free instance that cold-starts in ~50s, so all three "independent networks" were independent in location and identical in timeout. Then upgrade off Free regardless of the answer: a Free service sleeps when the store is quiet, so the first customer of the day waits ~50s at the till. |
| **Render: set the backend env vars** | `JWT_SECRET` and `DATABASE_URL` crash the server on boot if absent (`src/app.ts:129`). `WEBHOOK_SECRET_KEY`, `STRIPE_SECRET_KEY`, `CRON_SECRET`/`JOBS_TICK_SECRET` fail closed with 503. `ALLOWED_ORIGINS` must include `https://ascendhqweb.vercel.app` or the browser is CORS-blocked against a healthy backend. **Confirm `NODE_ENV=production` first** — it gates every one of those checks, so unset means the server boots misconfigured and silent. Full severity-tiered table now in `PIPELINE.md`. |
| **Supabase: which project is production, and seed the first tenant** | `DEPLOYMENTS.md` item 4. The project labelled *testing* (`lqaicxibgrlxwkvxsaji`) is the one carrying real schema and data; the one labelled *production* (`kplruangtivthgqudjwt`) has never been confirmed to receive a connection. Also allowlist Render's outbound ranges `74.220.50.0/24`, `74.220.58.0/24`, or `/readyz` fails against a healthy service. **And seed a first tenant/admin** — schema self-provisions on boot, so the likely landing state of a successful release is ~186 tables, zero users, and nobody able to log in. |
| **Repo variables + secrets** | `PROD_BACKEND_URL` (must be right *before* the release — `next.config.mjs` freezes it into `routes-manifest.json` at build time and it cannot be changed from the Vercel dashboard afterwards), `PROD_DEPLOY_TARGET=frontend`, `JOBS_TICK_SECRET` (also on Render — both halves required), and **`PROD_DATABASE_URL`, which is what makes backups real**: unset today, so `backup.yml` reports success in ~5s having backed up nothing and the RPO is unbounded, not ≤24h. |
| **`master` branch protection: correct the required check name** | Replace `Frontend — typecheck + lint + build` with `Frontend — typecheck + lint + test + build`, then **delete the `frontend-required-name-shim` job from `ci.yml`**. The shim added 2026-08-10 restores mergeability without touching protection, but it is a workaround, not the fix — the API returns 403 to agents. Keep `PIPELINE.md`'s list and the protection setting in sync. |
| ~~Merge `29831bd`~~ → fixes PORTED to feat/delivery-pipeline (2026-07-18) | Remaining for Sri: session C's clean-arch quotes pilot still lives only on `origin/feat/clean-arch-pilot-quotes` — review/merge separately; expect trivial same-change conflicts on the 10 vertical route files |
| Custom-roles / permissions-page contract | Decide the permission model: FE permissions matrix ({name,color,features} + bulk /settings/permissions) vs backend custom_roles ({name,permissions} fixed vocab, no color). Until then the paths stay allowlisted (AUDIT addendum §7) |
| Ecommerce storefront auth | DECIDED 2026-07-18: gated as Preview in the UI (`NEXT_PUBLIC_STORE_AUTH_ENABLED=1` re-enables). Build a real customer-auth backend when the storefront is prioritized |
| Catalog credits tab (`/catalog/:id/credits`) | No backing concept exists anywhere in the schema (not customer_invoices, not store credit) — needs a design decision on what a product-level "credit" even is (AR credit memo? something else?) before building. Left allowlisted, not invented |
| Inventory pipeline: Receiving tab | Implies a stateful "receiving session" (start receiving, scan qty progressively, track a receiver/batch_id) that doesn't exist — POs today go create → receive() in one atomic call, no partial in-progress session state. Needs a decision on whether that workflow is worth building before plumbing it |
| Inventory pipeline: Issues + Errors tabs | Both are GET+PATCH only in the FE (no POST anywhere) implying an unbuilt *detection* engine — categories like sku_mapping, price_mismatch, duplicate_doc, edi_parse that nothing in the codebase currently computes. Needs a decision on what should actually populate these (a rules engine? manual QA queue?) before building |
| Inventory pipeline: Overview/Summary funnel | FE's 9-stage funnel (suggested/draft/sent/confirmed/in_transit/partially_received/receiving/billed/closed) doesn't map onto the real 4-value POStatus enum (ordered/partially_received/received/cancelled), and several KPIs (overdue_pos, open_issues, receiving_active) depend on the three items above. Needs either a schema/status-model decision or FE simplification to match what the backend can actually report |
| Real EDI parsing (purchasing EDI-imports `/process` doesn't create real POs) | The Upload tab's `handleSubmit` (web/app/(protected)/purchasing/edi-imports/_components/UploadTab.tsx) POSTs only `{filename, format, supplier_id, supplier_name, file_size_bytes}` — the file's actual bytes are never read (no FormData/file.text()/base64) or sent anywhere, so no backend on any branch can parse real X12/EDIFACT/CSV content today; there is nothing to parse. `/process` therefore only performs a real status transition (valid → processed) and honestly returns `created_po_ids: []` rather than fabricating purchase orders. Closing this for real needs two decisions from Sri: (a) fix the frontend to actually upload file bytes (multipart or base64), and (b) pick either a real X12/EDIFACT parser library or a defined subset format (e.g. CSV-only) to build against — same class of product decision as catalog's `/credits` gap. Until both are decided, `/process` stays honest-but-inert for uploaded files (see src/modules/purchasing/edi-imports.ts's class-level doc comment) |
| Approval chains / run-history: real triggering-event wiring | Both approval_chains and workflow_run_history are real, persisted, tested — but nothing invokes them. Deciding which real POS/business action should check against which chain (a price override above X%? a refund above $Y? new vendor creation? a discount above Z%) and log a run — and what should happen while a transaction blocks awaiting approval (hold the sale? queue it? require a manager PIN inline?) — is a product decision, not plumbing, same class as catalog's `/credits` gap. Until decided, `runs` stays a real 0 and run-history stays a real empty table (see approval-chains.ts / run-history.ts class doc comments) |
| `npm test` locally | RESOLVED 2026-07-18: the earlier "sandbox can't run tests" note was an environment issue, not a hard limit — `npm install --no-save esbuild --force` re-fetches the correct platform binary and unblocks tsx/node:test in this Cowork sandbox. Used this session to verify 19 new catalog tests + regression-check team/customers/sso/orders/workforce, which is how the time_entries collision (below) was actually caught |
| ~~C-3 deploy note~~ | **RESOLVED, doubly stale.** PR #66 merged 2026-07-16 (iteration log entry, this file, line 44); C-3 itself (DB TLS cert verification) was separately fixed even earlier per `WORK/LOCK.md`'s "session D — C-3: verified DB TLS" claim (sslConfig verifies certs by default, `PG_CA_CERT_B64`/`PG_SSL_NO_VERIFY` escape hatches) — not re-verified this session, but no evidence of regression either. |
| C-2 completion | Confirm CRON_SECRET set in Vercel env — not re-verified this session |
| C-1 restore drill | **MECHANISM DRILLED 2026-08-05 — the prod half still blocked on Sri.** `db/backup/backup.sh` → `db/backup/restore.sh` were run end-to-end for the first time, version-matched (PostgreSQL 16 pair), against the real Ascend schema (193 tables from the app's own migrations) with seeded data: backup **0.168s → 501 KB**, restore **~1s** (script's own ≤30-min RTO gate PASSED), all 193 tables and every sampled row count identical (products/customers/orders/order_lines/payments/users), and the app booted against the **restored** DB with `/readyz` → `db:connected`. The scripts work. What is still NOT drilled is production, because there is nothing to restore from — see the row below. Evidence: `AUDIT_2026-08-05T054800Z-post-merge-staging-hardening.md` §5 |
| **Production backup has still never run (blocks any release rollback)** | Set repo secret `PROD_DATABASE_URL` (GitHub → Settings → Secrets and variables → Actions → Secrets) to the production Supabase Session-pooler connection string. Confirmed live 2026-08-05: `backup.yml` has reported **success 16 times** while skipping every real step (`Install postgresql-client`/`Run backup`/`Verify integrity`/`Upload artifact` all `skipped`, 5-second job) because the secret is unset and the workflow skips cleanly rather than failing. **Honest RPO is not ≤24h — it is total loss.** Verify with: Actions → "Database backup" → Run workflow, then confirm the run has a `db-backup-*` artifact attached |
| C-4 completion | Pick alert fan-out channel (Slack/PagerDuty/Sentry); heartbeat workflow is the floor — note the heartbeat itself has a confirmed, undiagnosed-until-2026-07-30 gap, see the new P0 row below; fan-out channel choice is a separate, still-open decision |
| OIDC PKCE + nonce | IdP-compatibility decision — not re-verified this session |
| ~~PR #66 merge~~ | **DONE 2026-07-16** — merged to master, deployed to prod (iteration log, line 44 above) |
| Cloud watchdog mode | Currently NOTIFY-ONLY (emails on stall). Optional upgrade to DO-WORK mode (cloud runs one iteration autonomously on stall) — true unattended continuity, but unreviewed cloud commits to financial code. Enable only if wanted. Routine: trig_01VVXryUgSBHoy9mAqRdhfzz |
| ~~NEW 2026-07-26 — Production heartbeat has gone silent~~ | **SUPERSEDED 2026-07-30 — this claim was wrong, not just stale.** The heartbeat never went silent; it fires roughly every 15 min continuously (confirmed via `gh run list`, dozens of runs through 2026-07-30) and has been failing the whole time on a now-understood cause: it probes a pre-migration Vercel backend URL that 404s. Full root cause, timeline, and open questions in `docs/architecture/DEPLOYMENTS.md` (P0-P3 fix plan) — that document supersedes this row and the 2026-07-26 gap audit's §4 on this topic. |
| ~~`master`/`staging` vs. `develop` sync (refreshed 2026-07-30)~~ | **RESOLVED 2026-08-02 — see new row below.** |
| **`master`/`staging` vs. `develop` sync (refreshed 2026-08-02)** | `develop → staging` promoted via PR #147 (60 commits, full package documented in `WORK/audits/AUDIT_2026-08-02T233841Z-develop-to-staging-promotion-package.md`). `develop` and `staging` are now in sync (0 apart); `staging` is 61 ahead of `master` (60 promoted commits + the merge commit itself) — promoting to `master` is Sri-only per `AGENTS.md`, not done here. Gates re-verified on the exact promoted tip before merging: backend typecheck, `hygiene` (1095 files), `table:scan` (161 names, no collisions), `gap:scan` (456/381, 21 allowlisted), frontend typecheck/lint/build — all clean. `deploy-staging`'s CI job is expected to fail on push (pre-existing dead Vercel target, see promotion package §2b) — not a code regression. |
| **NEW 2026-08-03 — incident: Replit migrated copy re-merged into `develop`** | `origin/develop` tip `74f7d91` again preserves the Replit pnpm/`artifacts/` workspace that PR #145 reverted. Root `package.json` requires pnpm; hygiene fails on duplicate `.migration-backup/AGENTS.md`; `npm run typecheck` at root is broken. Phase 13/14 dashboard work lived only under `artifacts/ascend/` — this session ports the command center into canonical `web/` (PR from `cursor/dashboard-display-data-45ad`). Full forward-only decontamination (same class as #145) needs Sri’s go-ahead; do not force-push. |
| **NEW 2026-08-02 — incident: Replit's migrated copy merged into `develop`, reverted** | The 2026-07-30 "Replit workspace is a separately migrated copy, not a live checkout" finding (row below) materialized into a real incident: a Replit session pushed commit `64758a5` merging its entire migrated-copy history (pnpm workspace, Drizzle, unrelated features) into `origin/develop`, deleting 395 files in `src/` and 427 in `web/` and replacing `package.json` with a bare workspace stub. Caught immediately (before any further merges landed on top) and fixed with a forward-only `git revert -m 1` (PR #145) — confirmed byte-for-byte identical to the pre-incident tip, no force-push, no history rewrite, nothing lost (the migrated-copy commits are still recoverable from `64758a5`'s second parent if ever wanted). `staging`/`master` were never touched. Underlying risk is unchanged — the same REPLIT.md git-safety rule this was supposed to prevent needs Replit sessions to actually follow it; consider this a live confirmation the rule is necessary, not proof it's now enforced. |
| **NEW 2026-07-30 — Production infrastructure reconciliation (P0)** | Three of this repo's own docs (`ARCHITECTURE.md`/`ORCHESTRATION.md`/`PIPELINE.md`) assert prod moved to Render 2026-07-20; zero deploy automation in `scripts/deploy.sh`/`ci.yml` supports that; the claimed prod URL is unreachable from three independent networks. Full investigation, evidence table, and fix plan in `docs/architecture/DEPLOYMENTS.md` — gated entirely on Sri confirming Render/Vercel/Supabase dashboard facts, not code-addressable. |
| **NEW 2026-08-07 — enable Dependency graph, then set `DEPENDENCY_REVIEW_ENABLED=true`** | `security.yml`'s dependency-review job failed in 7s on its first real run: *"Dependency review is not supported on this repository. Please ensure that Dependency graph is enabled."* The action needs GitHub's Dependency graph feature, which is a repo **setting**, not something a workflow can turn on. It is now gated behind a repo variable so it skips cleanly instead of always failing (same pattern as `deploy-dev`'s empty `DEV_BACKEND_URL` and `backup.yml`'s missing `PROD_DATABASE_URL`). To activate: enable Dependency graph at Settings → Code security, then set the repo variable `DEPENDENCY_REVIEW_ENABLED=true`. Until then the merged-state `npm audit` gate in `ci.yml` is the only dependency check, and it cannot see an advisory *arriving* on a PR. |
| **NEW 2026-08-06 — set `PROD_BACKEND_URL` / `PROD_FRONTEND_URL` repo variables** | `uptime.yml` already reads them and falls back to `ascendhq-api.vercel.app`, which no longer resolves — so the heartbeat has been failing on a DNS error in ~1s for ~30 consecutive runs. **A real outage currently looks identical to the status quo.** One-line fix once `DEPLOYMENTS.md`'s P0 answers what the real origins are. Settings → Secrets and variables → Actions → Variables. |
| **NEW 2026-08-06 — decide whether to extract `artifacts/` (1,005 files) to its own repo** | Five complete projects on an incompatible stack (Vite/Radix/Drizzle/pnpm workspace), **55% of this repo's tracked files**, built/tested/deployed by nothing. Direct cause of five root-manifest-hijack CI incidents in two days — `hygiene-check.mjs` check 8 now guards the symptom, but the cause is two incompatible project layouts in one tree. Also hides a finished, tested `push_tokens` module that ships to nobody. **Not actionable without Sri: `AGENTS.md` forbids deleting user work.** Recommendation is harvest-then-extract (own repo or orphan branch), never delete. |
| **NEW 2026-08-06 — `next` 14 → 16 is security remediation, not version hygiene** | The 10 `web` advisories (1 critical, 6 high) resolve only through this migration plus `vitest` 2 → 4. The `next` advisories specifically include **SSRF and HTTP request smuggling in `rewrites()`** — the mechanism this app proxies *all* backend traffic through (`next.config.mjs`) — and middleware-bypass/cache-poisoning against `middleware.ts`, which is this app's auth gate. Prioritise F-24/F-25 accordingly; fold nonce-based CSP into the same PR since it closes the `'unsafe-inline'` finding for free on Next 15+. |
| **NEW 2026-07-30 — GitHub repo default branch is still `master`** | Caused a real process violation this session (Cursor Cloud's PR #123 opened against `master` instead of `develop`, since tools bootstrap off whatever GitHub reports as default) despite `AGENTS.md` mandating `develop` as the start-work branch. Fix is a one-line repo setting (`gh repo edit --default-branch develop`) but wasn't executed pending Sri's confirmation — a repo-wide setting change, held the same way any other shared-state change was this session. |

## Delivery / release status (single live copy — refreshed 2026-07-30, was stale since 2026-07-19/20)

Rulebook: `docs/architecture/PIPELINE.md` (feature → develop → staging → master; master merges are Sri-only). This section is the live status; update in place. The 2026-07-19 table + 2026-07-25 note below are kept as history, not deleted — everything in them is long since resolved or superseded; don't act on any of it without cross-checking `WORK/LOCK.md` first.

| Item | State (2026-08-02, tier-sync/incident rows only — rest of table not re-audited this pass) |
|---|---|
| Tier sync | **Promoted.** `develop → staging` via PR #147 (60 commits) — `develop`/`staging` now in sync (0 apart), `staging` is 61 ahead of `master` (Sri-only to promote further, per `AGENTS.md`). Full package: `WORK/audits/AUDIT_2026-08-02T233841Z-develop-to-staging-promotion-package.md`. See the incident row above in the sync-status table above this section — the promotion happened right after reverting an accidental Replit migrated-copy merge into `develop` (PR #145), unrelated in cause but resolved in the same pass. |
| Open PRs (all → `develop`) | **2026-08-03T190400Z:** `develop` tip `a4dbf2c` is pnpm/`workspace` contaminated again (5th). Hotfix branch `cursor/hotfix-restore-npm-root-0e3c` restores npm root from `dcdf04b` + fixes `.cursor/environment.json` to npm ci. Supersedes stale #180/#173 for current tip. Do **not** merge workspace/pnpm trees. |
| Tier sync (2026-08-05) | **`develop → staging` promoted via PR #187** (103 non-merge commits, tip `f0c1845`). `staging..develop` = **0** (fully absorbed), `staging..master` = **0** and `develop..master` = **0** (invariant holds, no sideways drift), `master..staging` = **186** (unreleased). `git cherry origin/develop origin/staging` = 0 — the 4-commit `develop..staging` asymmetry is promotion merge-commit bookkeeping, not drift. All gates green on `f0c1845`: 852/852 backend tests, smoke, web typecheck/lint/build, and CI's own E2E golden paths (run 30977630220). **No back-merge was owed** — `master` has not moved since `e55e743` (2026-07-23). Promotion to `master` remains Sri-only and was NOT done. |
| Release readiness (2026-08-05) | **NO-GO on `staging → master`.** Code evidence is strong; the blockers are operational: (1) no production backup has ever run, so a data-affecting mistake in these 186 commits is permanent and there are no down-migrations in this repo; (2) the production heartbeat has 30 consecutive failures against a dead hostname, so a broken release would look exactly like today; (3) staging was never actually exercised — `deploy-staging` **ran and failed** (so `vars.STAGING_BACKEND_URL` *is* set; the Vercel projects it targets are gone), meaning no load/stress evidence exists at all. Full go/no-go evidence table: `AUDIT_2026-08-05T054800Z-post-merge-staging-hardening.md` |
| **Merge block found and worked around (2026-08-10)** | **`master` could not accept ANY merge between 2026-08-05 and 2026-08-10, and that — not the release-readiness NO-GO above — is why `master` sat at `e55e743`.** Commit `1a4b989` renamed the `Frontend — typecheck + lint + build` job to `… + test + build` without updating branch protection; a required check nothing emits is never satisfied and never fails, so the merge button was dead. GitHub's own answer on the PR #200 attempt: `405 Required status check "Frontend — typecheck + lint + build" is expected`. `ci.yml` now carries a `frontend-required-name-shim` job that re-emits the required name and mirrors the real job's result (`if: always()` + explicit assertion, so it cannot go green on a red frontend). **This restores mergeability; it does not merge anything, and `master` merges stay Sri-only.** The permanent fix (correct the protection name, delete the shim) is in NEEDS-SRI above. |
| **Release-path remediation MERGED to `develop` 2026-08-11 (`31c974c`, PR #211)** | Four agent-doable defects between "the release merges" and "the release works", all previously documented and none previously fixed: (1) the merge block above; (2) `deploy-production` deployed the backend to a Vercel project `DEPLOYMENTS.md` records as CONFIRMED BROKEN — default target changed to `frontend`, since Render's git integration owns the backend; (3) `vercel.json`'s `/jobs/tick` cron **could never reach a Render-hosted backend**, so the outbox never redelivered and no scheduled job ever ran — worse than the "~24h latency" GAPS.md recorded; replaced with `.github/workflows/jobs-tick.yml` (`*/15`, via ADR-012's `JOBS_TICK_SECRET` path); (4) the documented `{error:{code,message,requestId}}` envelope was never delivered because its middleware was unreachable dead code — folded into `errorMiddleware` additively (5xx `code` and validation `details` unchanged), and a second defect found alongside it: `contextFromRequest` reads request headers this app never sets, so **every 500 ever logged carried `requestId: undefined`** too. Status label: `built_verified` for the code (gates green, regression test proven to fail 4/4 pre-fix), `built_unverified` for production — nothing here has run against a production nobody has confirmed is alive. |
| **C-1 mechanism half closed (2026-08-11)** | The restore path is no longer taken on faith. `db/backup/drill.sh` runs fingerprint → backup → `--verify` → restore → compare → boot the app against the result → report RTO, and `.github/workflows/restore-drill.yml` runs it weekly **and on every change to `db/backup/**`** — the highest-risk moment for that machinery. The comparison is a per-table row count **and content md5**, so `users.password_hash` is proven intact byte for byte rather than merely present. Verified on real PostgreSQL 16: 193 tables / 324 rows identical, RTO 3s against a 1800s budget. **Both guards were proven to fail before being trusted**, and one of them was genuinely broken when first written: the post-restore verifier passed against a database with nothing restored into it, because `seedDemo()` self-creates the demo tenant whenever the users table is empty — fixed by requiring `NODE_ENV=production`. Also corrected a wrong instruction `restore.sh` had been giving operators since it was written: verify with `npm run smoke`, which provisions its own throwaway schema and never reads a restored row. **C-1's production half is UNCHANGED**: `PROD_DATABASE_URL` is still unset, so there is nothing to restore *from*. A green drill proves the path, not the artifact. Status label: `built_verified` for the mechanism, `missing` for production recovery. |
| **Rollback policy decided, not yet accepted (2026-08-11)** | [ADR-013](../docs/architecture/ADR/ADR-013-schema-is-forward-only-recovery-is-by-restore.md), status **Proposed**: schema is forward-only; recovery is a point-in-time restore, not a schema reversal; any destructive migration requires an explicit pre-migration backup checkpoint. The decisive argument against writing 53 sets of down-migrations is not cost — **a down-migration does not recover data.** Reversing a migration that dropped a column recreates an empty column. `PIPELINE.md`'s Rollback section is corrected: it previously opened with "no data migrations" and implied `git revert` on `master` rolled the database back, which is false for a release and worse than no rollback story because it read as solved. **Left Proposed, not Accepted** — a standing architectural commitment is Sri's call; everything mechanical it depends on is implemented and verified. |
| **Post-merge verification (2026-08-11)** | PR #211 merged to `develop` as `31c974c`. All three workflow runs green on the merge commit: **CI**, **Security**, and the new **DR drill (backup → restore → verify)** — 19 checks, 0 failing. **`E2E tests (Playwright — golden paths)` ran and PASSED on the push event** (run `31456534651`); it shows `skipped` on the concurrent `pull_request` run, which is expected — `e2e` is gated `if: github.event_name == 'push'`, so PR-level CI never covers it and e2e evidence only ever arrives *after* merge. Worth knowing before reading a green PR as fully covered. Deploy jobs skipped (pre-existing config; untouched here). Pre-merge audit additionally ran a real-stack Playwright suite locally — **29 passed / 1 skipped / 0 failed** against a production standalone frontend proxying a real backend — which caught and fixed a genuine pre-existing race in `inventory-receive.spec.ts`. `master` remains at `e55e743`: nothing here promoted or deployed to production. |
| Live investigation | Production infrastructure reconciliation — see `docs/architecture/DEPLOYMENTS.md`. Gated on Sri confirming Render/Vercel/Supabase dashboard facts (P0-P3 fix plan in that doc); nothing here is code-addressable until then. **Update 2026-08-10:** item 2 (where prod runs) is answered — Render "Ascend Prod", `srv-d9lo8jm7bikc739dnsn0`. Items 1 (does it answer), 3 (which deploy path is authoritative — now partly answered: Render git integration for the backend, Vercel for the frontend) and 4 (which database) stay open and are all in NEEDS-SRI above. |
| Prod URLs | **Frontend RESOLVED 2026-08-07 (Sri): `https://ascendhqweb.vercel.app`** — Vercel project `ascend_hq_web`, under the same scope CI already uses (`team_WNp8vBq1RmWTEH8WSnenP7jM` = `gellusricharan-4715s-projects`, corroborated by the preview URL `ascendhq-fkkm8rd5o-gellusricharan-4715s-projects.vercel.app`). The previously-published candidates (`ascendhq-app.vercel.app`, `finder-pos-frontend.vercel.app`) are dead and have been replaced repo-wide. **Backend still unresolved:** `ascend-prod.onrender.com` (per Sri, 2026-07-30) remains unresponsive from 3 independent networks, and `ascendhq-api.vercel.app` still returns `DEPLOYMENT_NOT_FOUND`. Do not trust the 2026-07-19 row below; it predates the Render migration claim entirely. |
| Coordination model | Four AI environments (Claude Code, Cursor, Replit) now coordinate via GitHub Issues (durable ownership) + short-lived `WORK/LOCK.md` locks + PRs — see `docs/architecture/ORCHESTRATION.md`'s "Environment routing" section. Replit's workspace was discovered 2026-07-30 to be a separately migrated copy, not a live checkout of this repo — its own task list (#5-#16 in its system) has no visibility from GitHub Issues here. |
| Branch cleanup | Not re-audited this session — the 2026-07-19 list below is old enough that most of it has likely already been resolved one way or another; don't act on it without re-checking `git branch -r` first. |

<details>
<summary>History: 2026-07-19 table + 2026-07-25 note (all resolved/superseded — kept for record, not for action)</summary>

| Item | State (2026-07-19 evening) |
|---|---|
| Flow correction | Sessions had routed work branch→staging, skipping develop (PR #70/#81 precedent). Sri ordered develop-first restored. Pending work rerouted onto `feature/web-error-details`; `feat/delivery-pipeline` retired (fully merged via PR #81). |
| In flight | `feature/web-error-details` (error.details adoption + boards + this consolidation) — awaiting Sri push, then PR → develop → staging. |
| develop sync | develop had fallen 33+ commits behind staging; fast-forward push (`origin/staging:develop`) in Sri's command queue. Invariant from now on: develop ≥ staging ≥ master. |
| Release | PR #82 (staging → master) open, awaiting Sri's merge. On merge prod auto-deploys; then verify ascendhq-api /healthz sha + /readyz db:connected; browser smoke: invoice WITH line item (was 500), quotation (was dead), workforce clock-in (was orphaning). |
| Prod URLs | ascendhq-app.vercel.app (frontend) / ascendhq-api.vercel.app (backend) — post-rebrand names, both verified live at the time. Legacy git-connected Vercel project `finder-pos` is cruft → NEEDS-SRI deletion. |
| Branch cleanup | Delete (fully merged into staging): feat/delivery-pipeline, backend-cycle3, feat/enterprise-pipeline, fix/deploy-sh-independent-targets, fix/prod-token-separation, rebrand/{client-storage-keys,cookie-dual-write,demo-credentials,live-urls}. Sri-triage before deleting (unmerged diffs): 12× worktree-agent-*, 11× wt/issue-*, Sricharangellu-patch-1/2, chore/retire-phantom-accounting-workflow, claude/finder-pos-assessment, feat/clean-arch-pilot-{expenses,quotes}, feat/demo-tenant-lifecycle, feat/m1-b2b-accounts, fix/{api-pagination-and-delete-consistency,gate-money-movement-mutations,retail-package-isolation,sso-oidc-verification}, harden/{money-path-coverage,webhook-secret-fail-closed}, merge/pr78-refund-ledger. |
| CI fix shipped | Postgres service containers now get --shm-size=1g (e0d0035) — 64MB Docker default was killing the 700-test suite mid-run. |

**Note (2026-07-25):** the table above is stale (last touched 2026-07-19/20) —
current reality per `WORK/LOCK.md`'s later entries: work moved to
`feature/retire-inventory-expiry-page` (local) / `feature/reliability-phase4a`
(origin), which is now several commits ahead of what this table describes
(circuit breaker, inventory reconciliation, payment gateway seam, business-pack
matrix, reports module review, and PR #120 opened into `develop` — see LOCK.md
for the full trail). This session added one more commit on the same branch:
finished + verified an in-progress, previously-uncommitted `ai_assistant`
module (ADR-005) that was sitting in the working tree with no LOCK claim and
no tests — fixed Design System Rules violations in its page, added 13 new
backend tests (43/43 incl. regression), all gates clean. See `WORK/LOCK.md`'s
"finish + verify AI Assistant module" claim for detail.

</details>
