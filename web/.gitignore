# Next.js
.next/
out/

# Dependencies
node_modules/

# Environment files (never commit secrets)
.env.local
.env.*.local

# MSW service worker (generated by `npx msw init public/`)
public/mockServiceWorker.js

# Build artifacts
*.tsbuildinfo

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/settings.json
.vercel
# See the root .gitignore: the bare `.env*` must keep a negation for the
# committed templates, or deleting `web/.env.example` becomes irreversible.
.env*
!.env*.example
