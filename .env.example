# Postgres connection string. Local dev/tests fall back to an embedded Postgres
# when this is unset; production (Vercel) requires it.
# Example (Neon): postgresql://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL=
